int cond(bool b)
{
  int x;
  if (false)
    x = 42;
  elif (b) /* because this is an if statement whose condition evaluates to true, the below elif statement is skipped */
    x = 95;
  else if (b)
    x = 423;
  elif (b)
    x = 500;
  return x;
}

int main()
{
 print(cond(false));
 return 0;
}
